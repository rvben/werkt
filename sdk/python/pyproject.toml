[project]
name = "werkt-sdk"
version = "0.2.0"
description = "Dependency-free Python authoring SDK and connectors for Werkt automations"
license = "Apache-2.0"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
