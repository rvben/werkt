[project]
name = "werkt-sdk"
version = "0.1.0"
description = "Python adapter for the Werkt process protocol"
license = "Apache-2.0"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
